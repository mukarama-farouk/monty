#include "monty.h"
/**
  *f_nop- does nothing
  *@head: head of stack
  *@lineCount: line_number
  *Return: no return
 */
void f_nop(stack_t **head, unsigned int lineCount)
{
	(void) lineCount;
	(void) head;
}
